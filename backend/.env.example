MONGODB_URI=mongodb+srv://career_user:Career1234@cluster0.8vmqsmp.mongodb.net/?appName=Cluster0
JWT_SECRET=your_jwt_secret_key_change_in_production
PORT=5000
NODE_ENV=development

# Prisma (SQLite by default — file lives next to prisma/schema.prisma)
DATABASE_URL="file:./dev.db"

# Browser origin for CORS + Socket.IO (Vite dev server)
CLIENT_ORIGIN=http://localhost:5173
