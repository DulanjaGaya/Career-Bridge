# SQLite (default — works immediately)
DATABASE_URL="file:./dev.db"

# PostgreSQL example (change provider in prisma/schema.prisma to postgresql)
# DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/career_readiness?schema=public"

PORT=4000
CLIENT_ORIGIN=http://localhost:5173
